package com.example.yapeapp.helpers

object Constants {
    const val BASE_URL = "http://http://demo9207776.mockable.io/"

    enum class ApiError(var error: String) {
        API_ERROR("Error al obtener las recetas")
    }
}